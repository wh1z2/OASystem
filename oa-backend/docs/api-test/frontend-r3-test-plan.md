# R3 前端权限控制修复 - 页面测试方案

**文档版本**: v1.0
**编制日期**: 2026-04-15
**对应风险项**: R3 - 前端无任何角色权限控制，所有菜单和按钮对所有用户可见

---

## 测试目标

验证本次 R3 修复后，前端各页面能够根据用户角色和权限正确控制：
1. 侧边栏菜单的显示与隐藏
2. 路由访问的拦截与跳转
3. 页面内操作按钮的显示与隐藏
4. 权限变更后的界面联动响应

---

## 测试环境

| 项目 | 说明 |
|------|------|
| 测试账号 | admin/admin123, manager/manager123, user/user123 |
| 浏览器 | Chrome / Edge 最新版 |
| 测试方式 | 手工功能测试为主，配合浏览器 DevTools 模拟数据 |
| 前置条件 | 后端服务已启动，数据库角色权限配置正确 |

**数据库预期权限配置**:
- admin: `["all", "approval:execute", "approval:execute:all", "approval:view:all"]`
- manager: `["approval:execute", "approval:execute:dept", "approval:view:all", "user_view", "report"]`
- employee: `["apply", "personal"]`

---

## 页面元素验证

### 登录页 (Login.vue)

| 用例编号 | 测试点 | 操作步骤 | 预期结果 | 结果 |
|----------|--------|----------|----------|----------|
| FE-LOGIN-001 | 快捷登录入口 | 观察登录页底部快捷登录按钮 | 显示 admin、manager、employee 三个快捷入口 | √ |
| FE-LOGIN-002 | admin 登录 | 点击 admin 快捷入口并登录 | 跳转至工作台，侧边栏显示全部菜单 | √ |
| FE-LOGIN-003 | manager 登录 | 点击 manager 快捷入口并登录 | 跳转至工作台，仅显示有权限的菜单 | √ |
| FE-LOGIN-004 | employee 登录 | 点击 employee 快捷入口并登录 | 跳转至工作台，仅显示工作台、审批流程（发起审批）和个人中心 | ×<br />虽然侧边栏不显示无权限的菜单项了，但是工作台页面的快捷操作里面依旧有代办审、已办事项、表单设计，而且个人中心的快捷操作里也包含待办事项、已办事项 |

### 主布局 - 侧边栏菜单 (MainLayout.vue)

| 用例编号 | 角色 | 测试点 | 预期结果 | 结果 |
|----------|------|--------|----------|----------|
| FE-MENU-001 | admin | 菜单完整性 | 显示：工作台、待办事项、已办事项、审批管理（审批流程、表单设计器）、系统管理（用户管理、角色权限） | √ |
| FE-MENU-002 | manager | 菜单过滤 | 显示：工作台、待办事项、已办事项、审批流程、用户管理；**不显示**：表单设计器、角色权限 | ×<br />工作台页面的快捷操作里面依旧有表单设计 |
| FE-MENU-003 | employee | 菜单过滤 | 显示：工作台、审批流程；**不显示**：待办事项、已办事项、表单设计器、用户管理、角色权限 | ×<br />工作台页面的快捷操作里面依旧有代办审、已办事项、表单设计，而且个人中心的快捷操作里也包含待办事项、已办事项 |
| FE-MENU-004 | admin | 菜单徽章计数 | 待办/已办/审批流程的数字徽章正常显示 | √<br />--需要补充：admin可以审批所有工单，无论审批人是不是admin，也不论是否跨部门，admin账号就是可以拥有所有操作的权限 |
| FE-MENU-005 | employee | 无权限菜单隐藏 | 页面源码或 DOM 中不包含 `/users`、`/roles`、`/form-designer` 的 router-link | √ |

### 审批管理页 (ApprovalManage.vue)

| 用例编号 | 角色 | 测试点 | 预期结果 | 结果 |
|----------|------|--------|----------|----------|
| FE-APP-001 | admin/manager | "审批"按钮显示 | 对于 `status=processing` 的工单，显示"审批"按钮 | √ |
| FE-APP-002 | employee | "审批"按钮隐藏 | 对于 `status=processing` 的工单，**不显示**"审批"按钮，仅显示"查看" | √ |
| FE-APP-003 | admin/manager/employee | "发起审批"按钮 | 有 `apply` 权限时显示"发起审批"按钮；无 `apply` 权限时不显示 | √ |
| FE-APP-004 | employee | 列表数据查看 | 可以查看审批列表（有 `apply` 权限即可访问页面） | √ |
| FE-APP-005 | manager | 审批弹窗操作 | 点击"审批"按钮可正常打开弹窗，通过/拒绝按钮可用 | ×<br />测试张经理账号无法进行审批 |

### 审批详情页 (ApprovalDetail.vue)

| 用例编号 | 角色 | 工单状态 | 测试点 | 预期结果 | 结果 |
|----------|------|----------|--------|----------|----------|
| FE-DET-001 | admin/manager | processing | 审批操作区 | 显示"审批意见"输入框、"通过审批"和"拒绝审批"按钮 | √ |
| FE-DET-002 | employee | processing | 审批操作区 | 显示"您没有该工单的审批权限"提示，不显示操作按钮 | √ |
| FE-DET-003 | admin/manager/employee | draft | 提交操作 | 有 `apply` 权限时显示"提交审批"按钮；无权限时仅显示"草稿待提交"文本 | ×<br />--manager无法发起审批，其他符合预期 |
| FE-DET-004 | 任意角色 | approved/returned/revoked | 已完成状态 | 显示"该审批已处理完成"，无任何操作按钮 | √ |
| FE-DET-005 | 任意角色 | 任意 | 审批历史 | 审批历史记录正常显示 | 待定 |

### 用户管理页 (UserManage.vue)

| 用例编号 | 角色 | 测试点 | 预期结果 | 结果 |
|----------|------|--------|----------|----------|
| FE-USER-001 | admin | 完整权限 | 显示"添加用户"按钮，列表中每行显示"编辑"、"删除"按钮 | √ |
| FE-USER-002 | manager | 查看权限 | 可访问页面查看用户列表，但**不显示**"添加用户"、"编辑"、"删除"按钮 | √ |
| FE-USER-003 | employee | 无权限访问 | 通过路由守卫拦截，自动跳转至工作台首页 | √ |

### 角色权限管理页 (RoleManage.vue)

| 用例编号 | 角色 | 测试点 | 预期结果 | 结果 |
|----------|------|--------|----------|----------|
| FE-ROLE-001 | admin | 完整权限 | 显示"添加角色"按钮，卡片上显示"编辑"、"删除"图标按钮 | √ |
| FE-ROLE-002 | manager | 无权限访问 | 通过路由守卫拦截，自动跳转至工作台首页 | √ |
| FE-ROLE-003 | employee | 无权限访问 | 通过路由守卫拦截，自动跳转至工作台首页 | √ |
| FE-ROLE-004 | admin | 删除保护 | admin 角色卡片上不显示删除按钮（原有逻辑保留） | √ |

---

## 用户交互流程测试

### 路由权限拦截流程

| 用例编号 | 角色 | 操作步骤 | 预期结果 | 结果 |
|----------|------|----------|----------|----------|
| FE-ROUT-001 | employee | 直接浏览器地址栏输入 `/users` | 自动跳转回 `/`（工作台），不显示 404 | √ |
| FE-ROUT-002 | employee | 直接浏览器地址栏输入 `/roles` | 自动跳转回 `/`（工作台） | √ |
| FE-ROUT-003 | manager | 直接浏览器地址栏输入 `/form-designer` | 自动跳转回 `/`（工作台） | √ |
| FE-ROUT-004 | employee | 直接浏览器地址栏输入 `/todo` | 自动跳转回 `/`（工作台） | √ |
| FE-ROUT-005 | admin | 直接浏览器地址栏输入 `/users` | 正常加载用户管理页面 | √ |
| FE-ROUT-006 | 未登录 | 直接浏览器地址栏输入 `/approval` | 跳转至 `/login` 登录页 | √ |
| FE-ROUT-007 | 已登录 | 访问 `/login` | 自动跳转至 `/`（工作台） | √ |

### 权限联动流程

| 用例编号 | 场景 | 操作步骤 | 预期结果 | 结果 |
|----------|------|----------|----------|----------|
| FE-FLOW-001 | 登出后权限清除 | admin 登录后点击退出，再直接访问 `/users` | 跳转至登录页 | √ |
| FE-FLOW-002 | Token 过期 | 模拟 Token 过期（如修改 localStorage 中的 token），刷新页面 | 自动清除登录状态并跳转至登录页 | √ |
| FE-FLOW-003 | 多标签页同步 | 在标签页 A 登出，标签页 B 刷新 | 标签页 B 跳转至登录页 | √ |

---

## 五、边界条件处理

| 用例编号 | 场景 | 操作步骤 | 预期结果 |
|----------|------|----------|----------|
| FE-EDGE-001 | 空权限列表 | 模拟后端返回 `permissions: []` | 仅显示所有登录用户可见的页面（工作台、个人中心） |
| FE-EDGE-002 | 未知权限码 | 模拟后端返回 `permissions: ["unknown_perm"]` | 不因此报错，按实际权限匹配结果渲染 |
| FE-EDGE-003 | permissions 字段缺失 | 模拟旧版本后端（无 permissions 字段） | `authStore.permissions` 默认为空数组，不会导致 JS 报错 |
| FE-EDGE-004 | `all` 权限通配 | admin 拥有 `all` 权限 | 视为拥有所有权限，所有菜单和按钮均可见 |
| FE-EDGE-005 | 大小写敏感 | 权限码区分大小写 | `All` 不等于 `all`，按字符串精确匹配 |

---

## 六、异常场景模拟

| 用例编号 | 场景 | 操作步骤 | 预期结果 |
|----------|------|----------|----------|
| FE-EXC-001 | 后端 403 响应 | employee 尝试通过 API 调用来审批工单（前端已隐藏按钮） | 前端弹出"没有权限执行此操作"提示（由 apiClient 拦截器统一处理） |
| FE-EXC-002 | 后端 /auth/info 500 | 登录成功后 `/auth/info` 接口异常 | 前端自动清除登录状态并跳转登录页 |
| FE-EXC-003 | 网络中断 | 登录过程中断网 | 显示"网络连接失败，请检查网络设置" |
| FE-EXC-004 | 越权 URL 访问 | 使用浏览器开发者工具修改 `localStorage` 中的 user 对象，伪造 admin 权限后访问页面 | 由于 `initAuth()` 会调用 `/auth/info` 重新校验，伪造数据会被覆盖；若拦截失败，后端接口会返回 403 |

---

## 七、测试执行建议

1. **第一轮**：使用三个标准测试账号逐一登录，截图记录各角色看到的侧边栏菜单状态。
2. **第二轮**：在每个角色下，访问所有路由 URL（`/`, `/todo`, `/done`, `/approval`, `/approval/create`, `/form-designer`, `/users`, `/roles`, `/profile`），记录拦截行为。
3. **第三轮**：在审批管理页和审批详情页，针对不同工单状态（draft, processing, approved, returned），检查操作按钮的显示状态。
4. **第四轮**：使用浏览器 DevTools 模拟异常情况（如清空 permissions、修改 role、断网等），验证边界处理和异常提示。

---

## 八、通过标准

- [ ] 所有角色的菜单显示符合权限矩阵
- [ ] 无权限路由访问均被拦截且无报错
- [ ] 审批操作按钮按权限和状态正确显隐
- [ ] 用户/角色管理页面操作按钮按权限正确显隐
- [ ] 异常场景下前端不崩溃，有合理的提示或跳转





有以下问题： 1.登录张经理账号，点击待办事项后显示暂无待办事项（表里有一个部门为财务部，审批人为张经理的工单），如附件图1，然后点击了审批流程之后又能显示出待办事项了，如附件图2。 2.现在表里有一个财务部的工单待审批，审批人是张经理，张经理本身是技术部的，工单详情页点击通过审批或者是待办事项页面点击快速审批都无反应，后端控制台输出权限不足如附件图3 3.当我登录普通员工账号时，虽然侧边栏不显示无权限的菜单项了，但是工作台页面的快捷操作里面依旧有代办审、已办事项、表单设计，并且个人中心的快捷操作里也包含待办事项、已办事项。如果决定待办事项和已办事项不对普通员工显示，那么普通员工的工作台页面的已通过、已拒绝是否也可以隐藏，仅显示本月申请？ 4.需要补充一条规则：admin可以审批所有工单，无论审批人是不是admin，也不论是否跨部门，admin账号就是可以拥有所有操作的权限，所以admin的待办事项里面应该显示所有“审批中”的工单。 5.部门经理应该添加表单设计器的权限 6.当前重新编辑工单的设计是：可以对已通过或已打回的工单重新编辑，状态变回草稿，但是我觉得不太合理，已通过的工单不应可以再被编辑，所以设计应该变为可以对草稿或已打回的工单重新编辑 7.当前的待审批人的指定逻辑我觉得是缺失的，虽然接口测试里可以在请求体里面设置待审批人，但是在前端页面中给发起审批人指定待审批人显然是不合理的，所以需要做到一个配置逻辑，默认情况下应该怎么指定审批人 8.我觉得审批流程和已办事项盘的颜色徽章是没必要的，可以去掉，仅需保证显示待办事项旁的徽章正确即可（不被其他列表查询操作影响） 9.数据库权限表的内容还可以完善，目前部门经理竟然无法发起审批，不管是工作台页面快捷操作里的发起审批还是个人中心快捷操作里的发起审批都无法发起 10.在前端页面中没有看到有“撤销（REVOKE）”的操作，疑似缺失 11.仔细检查待办事项、已办事项、审批流程三个模块的前端代码逻辑。以待办事项、登录账号为张经理为例，我点击待办事项后只显示一条数据，利用浏览器开发者工具抓包得到此时的网络调用是调用了TodoList.vue而不是后端接口，这是严重错误。已办事项也是类似逻辑，无法显示正确数据。然后当我点击审批流程后再回去查看待办事项、已办事项时发现此时的查询结果才正确。 12.已办事项的筛选功能有误，当筛选下拉列表里有已通过和已拒绝，应该是已拒绝和已打回冲突了，后端规定的是拒绝后的工单状态时已打回而不是已拒绝，导致筛选不出。